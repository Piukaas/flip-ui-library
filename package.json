{
  "name": "flip-ui-library",
  "version": "1.0.0",
  "scripts": {
    "docs:generate": "node scripts/generate-docs.js",
    "css:minify": "node scripts/minify.js",
    "docs:dev": "npm run docs:generate && vitepress dev docs",
    "docs:build": "npm run docs:generate && vitepress build docs && npm run css:minify",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.0.0"
  },
  "supportedArchitectures": {
    "os": [
      "current",
      "linux"
    ],
    "cpu": [
      "current",
      "x64"
    ]
  }
}